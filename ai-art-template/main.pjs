// AI Art Template — a clean, simple AI image generator.
// No style presets are configured; the raw user prompt is used directly.

generateImage = {import:text-to-image-plugin}

$meta
  title = AI Art Template
  description = Simple AI art generator with no style presets
  tags = ai art, text to image, image generator
